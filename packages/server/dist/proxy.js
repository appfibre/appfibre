"use strict";
/*

{ "@cdnjs": location.hostname === "localhost" ? "http://localhost/cdn/cdnjs" : "https://cdnjs.cloudflare.com/ajax/libs"
, "@appfibre": location.hostname === "localhost" ? "http://localhost/cdn/appfibre" : "https://unpkg.com/@appfibre/bundles@0.0.4/"
}

*/
