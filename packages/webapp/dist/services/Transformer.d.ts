import { types } from "@appfibre/types";
import { Services } from "@appfibre/core";
export declare class Transformer extends Services.Transformer {
    constructor(settings?: types.app.ITransformSettings);
}
